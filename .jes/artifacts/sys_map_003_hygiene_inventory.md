# SYS-MAP-003 — Hygiene Inventory (Part B)

**Date:** 2026-08-10
**Scope:** `src/jarvis/` (production code); tests referenced only as evidence of deadness/liveness, never as an audit target themselves.
**Rule enforced throughout:** report-only. No deletion, no refactor, no behavior change. `git status --short -- src/` is empty for this entire session.

## Labels

`DEAD` (no callers, safe future-delete candidate) · `RESIDUAL` (leftover from closed work) · `DUPLICATE` (two+ implementations of the same non-trivial rule) · `REDUNDANT` (abstraction/branch no longer earning its complexity) · `SUSPECT` (looks dead/duplicate but needs an Engineer call)

## Full catalog

| ID | Label | Location | Evidence | Risk if wrong | Recommended next | Effort |
|---|---|---|---|---|---|---|
| HYG-001 | DEAD | `adapters/cli/main.py::render_response`, lines 241-249 | The generic `if result.get("status") == "interactive":` branch at line 212 already returns unconditionally for any interactive-status result, including ones with `action == "define_missing_params"`. The later `if result.get("action") == "define_missing_params": if result.get("status") == "interactive": ...` block can never execute that inner branch — status is never still `"interactive"` by the time control reaches it. | Very low — purely dead, removing it changes nothing observable. If somehow wrong (e.g. a future refactor reorders the checks), the risk is silent behavior change, not a crash — worth a regression test if ever touched. | Leave for a future hygiene FN; delete the unreachable inner `if` (keep the outer one only if a non-interactive `action == "define_missing_params"` case still needs the fallthrough comment it currently has). | S |
| HYG-002 | SUSPECT | `core/mutation_engine.py::apply_mutation` (module-level function, lines 404-408) | Zero production callers (`orchestrator.py`, `actions/iterate.py` both call `MutationEngine().apply_mutation(...)`, the class method, directly). Only reference anywhere is `tests/test_mutation_engine.py:23`, which imports and calls the free function once. | Low-medium — could be intentional public/back-compat API surface for external callers or future use; deleting without checking intent could break an implicit contract even though nothing in this repo currently needs it. | Ask Engineer whether this is deliberate public API (keep, maybe document why) or truly vestigial (fold the one test onto the class method, then delete). Do not delete unilaterally. | S |
| HYG-003 | RESIDUAL | `core/intent_resolver.py` — `IntentType` `Literal[...]` (lines 10-22) vs. `_resolve_strong_action_intent` (line 498) | `_resolve_strong_action_intent`, whose return type is annotated `IntentType \| None`, can return the literal string `"dismiss_suggestion"` (Bug 49) — but `IntentType`'s `Literal[...]` definition does not include `"dismiss_suggestion"` as a member. No runtime impact (Python doesn't enforce `Literal` at runtime), but a real static-typing correctness gap; a type checker (mypy/pyright), if ever added to CI, would flag every call site that pattern-matches on `IntentType`. | Low today (no CI type-checking observed in this repo); would become a real (if easy) finding the moment static typing is enforced. | Add `"dismiss_suggestion"` to the `IntentType` Literal. One-line, zero behavior change, purely a type-annotation fix — arguably fits within this contract's §6 "docstring/comment corrections that remove false claims," but flagged here rather than fixed inline since it touches a type alias, not a comment, so left for Engineer sign-off per contract's narrow-edit rule. | S |
| HYG-004 | DUPLICATE | `orchestrator.py` (≈8 sites: lines 806, 1445, 1691, 1716, 1740, 1838, 2425, 2495) + `utils/design_utils.py` (4 getters: `get_frame_material`, `get_frame_mass_kg`, `get_battery_capacity_wh`, `get_motor_power_w`) | All independently inline the same one-line predicate `spec is None or spec.completeness == "low"` (or the `!= "low"` negation) instead of calling `project_closure.component_presence_tier(spec)` — the exact primitive FN-020 introduced specifically to be the single source of truth for this threshold. Not currently a bug (every site computes the identical result — verified by inspection, no divergence found), but it is precisely the *shape* of scattered-threshold duplication FN-020 was created to eliminate for the BOM/architecture split; a future edit to one site without the others would silently reintroduce an FN-020-class contradiction. | Medium if ignored long-term — the risk is not "wrong today" but "easy to make wrong tomorrow" by editing one call site and missing the other eleven. | Candidate for a future hygiene FN: replace all ~12 inline checks with `component_presence_tier(...) == "stub"` (or `!= "stub"`). Mechanical, low-risk, high test coverage already exists (full suite would catch any behavior change immediately since the substitution is semantically identical). | M |
| HYG-005 | REDUNDANT | `core/component_writers.py::apply_components_delta`, lines 259-280 | Self-documented by its own author (`# TODO (v2 — refactor semántico)` comment): the function does two conceptually distinct things (apply a real delta vs. baseline-normalize when delta is `{}`), with an explicitly flagged risk ("un caller que pase {} esperando no-op obtendrá recálculo completo"). Verified: all 3 current callers (`design_explorer.py` ×2, `orchestrator.py::_handle_apply_exploration` ×1) already expect the "always recompute" behavior — the flagged risk has not yet materialized. | Low today (no divergent-expectation caller exists yet); the comment itself says "no cambiar hasta que haya un segundo caller con expectativa distinta" — i.e. the author already decided this is not urgent. | Leave as-is per the author's own note; revisit only if/when a caller wanting true no-op-on-empty-delta semantics appears. | M (if ever split) |
| HYG-006 | RESIDUAL | `core/design_explorer.py::_evaluate`'s param-hash cache, lines 398-409 | Self-documented (`# TODO (v2)`): the cache key assumes an injective component→params mapping; two different `ComponentSpec` combinations that happen to derive identical `current_parameters` would collide and the second candidate would silently reuse the first's cached result. Author's own note: "Impacto actual: bajo (grids con valores muy distintos)." | Low, self-assessed by the author as low-impact given current `EXPLORATION_GRIDS` value spread. | Leave as-is; revisit if `EXPLORATION_GRIDS` is ever extended with closely-spaced values that could plausibly collide. | M (if ever fixed — needs component identity in the cache key) |
| HYG-007 | DUPLICATE | `suggestions/suggestion_engine.py` (`HIGH_MARGIN_THRESHOLD=1.8`, `LOW_MARGIN_THRESHOLD=1.3`, `HIGH_TW_RATIO_THRESHOLD=1.6`) vs. `core/goal_planner.py::_prioritize_strategies` (inline `1.15`/`1.5` cutoffs) | Two independent, non-communicating "is this margin risky" threshold tables, each legitimate for its own purpose (general post-sim suggestions vs. payload-goal strategy reordering) — not a bug, no divergent behavior observed, but two sources of truth for a similar-sounding concept. **Directly relevant to H5** (`MISMATCHES.md`), which will need to pick a single "risky margin" cutoff for Continuity's next-step text — whoever picks up H5 should read both tables first rather than inventing a third number. | Low today; medium relevance the moment H5 is implemented (risk of introducing a *third* independent threshold). | No code change recommended. Cross-reference this finding explicitly from any future H5 design/implementation contract (already noted in `07_simulation/SIMULATION_MAP.md` since SYS-MAP-002; reaffirmed here). | N/A (awareness only) |
| HYG-008 | DEAD | `knowledge/loader.py` | 0 bytes. Zero references (`from jarvis.knowledge.loader` / `knowledge import loader` / bare `loader`) anywhere in `src/` or `tests/`. | None — empty file, nothing to break. | Candidate for deletion in a future Engineer-approved hygiene FN (batch with HYG-009…016). | S |
| HYG-009 | DEAD | `knowledge/parser.py` | 0 bytes. Zero references anywhere in `src/` or `tests/`. | None. | Same as HYG-008. | S |
| HYG-010 | DEAD | `knowledge/retriever.py` | 0 bytes. Zero references anywhere in `src/` or `tests/`. | None. | Same as HYG-008. | S |
| HYG-011 | DEAD | `simulation/energy_model.py` | 0 bytes. Zero references anywhere. All energy-model math actually lives directly in `simulation/simulator.py`. Was mischaracterized as active in SYS-MAP-002 (see `MISMATCHES.md` M-004, corrected in this pass). | None. | Same as HYG-008. | S |
| HYG-012 | DEAD | `simulation/flight_model.py` | 0 bytes. Zero references anywhere. All flight-physics math actually lives directly in `simulation/simulator.py`. Was mischaracterized as active in SYS-MAP-002 (M-004). | None. | Same as HYG-008. | S |
| HYG-013 | DEAD | `tools/materials.py` | 0 bytes. Zero references anywhere. Was mischaracterized as "consumed by the engine" in SYS-MAP-002 (M-004). | None. | Same as HYG-008. | S |
| HYG-014 | DEAD | `tools/math_utils.py` | 0 bytes. Zero references anywhere. Was mischaracterized as "consumed by the engine" in SYS-MAP-002 (M-004). | None. | Same as HYG-008. | S |
| HYG-015 | DEAD | `utils/helpers.py` | 0 bytes. Zero references anywhere in `src/` or `tests/`. | None. | Same as HYG-008. | S |
| HYG-016 | DEAD | `utils/validators.py` | 0 bytes. Zero references anywhere in `src/` or `tests/`. | None. | Same as HYG-008. | S |

**Method note for HYG-008…016:** found via `find src/jarvis -name "*.py" -size 0`, which also correctly excluded legitimate empty `__init__.py` package markers (`adapters/__init__.py`, `adapters/cli/__init__.py`) — those are not findings, they're normal Python package structure. Each of the 9 listed above was then independently confirmed via `grep -rn "<module_name>" src/jarvis tests --include="*.py"` returning zero matches outside the file itself.

## Top 10, ranked by (confidence × value)

Confidence is high (grep/AST-verified, not inferred) for every item below; ranking is therefore driven by value — specifically, how much a future reader or FN author benefits from knowing this now.

| Rank | ID | Why it ranks here |
|---|---|---|
| 1 | **HYG-007** | Directly informs a near-future decision (H5's margin cutoff) — highest "prevents a future mistake" value of anything in this catalog. Not blocking, but the most consequential to flag. |
| 2 | **HYG-004** | The FN-020-class duplication pattern (~12 sites). Doesn't block anything today; meaningfully reduces regression risk for any future Continuity/Acquisition-adjacent FN if unified. |
| 3 | **HYG-008…016 (as one cluster)** | 9 confirmed-empty, confirmed-unused files. Trivial, zero-risk, but real: cleanest possible "quick win" for a future hygiene FN, and two of them (HYG-011/012) actively caused a documentation error (M-004) this pass had to correct. |
| 4 | **HYG-005** | Self-flagged by original author with a concrete failure scenario; worth keeping visible even though the trigger condition hasn't fired. |
| 5 | **HYG-002** | The one item that genuinely needs an Engineer judgment call (public API vs. vestigial) rather than a mechanical fix — ranked here because it's the only SUSPECT in the catalog. |
| 6 | **HYG-001** | Purely cosmetic dead code, zero behavior risk, but easy and safe to clean up whenever `adapters/cli/main.py` is next touched. |
| 7 | **HYG-003** | Trivial type-annotation gap, zero runtime impact — low urgency but genuinely correct to fix, and cheap. |
| 8 | **HYG-006** | Self-flagged, author-assessed as low-impact; lowest priority of the "real" findings. |

## Does this block future handoff FNs?

**No.** Nothing in this catalog blocks H1/H2/H3/H4/H5 or Create→BOM. HYG-007 is *relevant context* for H5 (not a blocker — H5 is already deferred for its own, separate data-contract reason per `MISMATCHES.md`). HYG-004 is general hygiene, not scoped to any pending handoff edge. Everything else is cosmetic or genuinely inert (empty files).

## Cosmetic vs. substantive (explicit split, per contract)

- **Cosmetic (no functional risk, purely code cleanliness):** HYG-001, HYG-003, HYG-008 through HYG-016 (11 items).
- **Substantive (real design-debt signal worth an Engineer's attention, even though none are urgent):** HYG-002 (needs a decision), HYG-004 (pattern worth unifying before it drifts), HYG-005, HYG-006 (both self-flagged, low current risk), HYG-007 (relevant to a specific future decision, H5).

## Hygiene follow-up FN recommended?

Optional, not urgent. If the Engineer wants one: a single small hygiene FN covering HYG-001 (delete dead branch), HYG-003 (add missing Literal member), and HYG-008…016 (delete 9 empty files) would be a clean, low-risk, high-confidence batch — all mechanical, all independently verified zero-impact in this report. HYG-002/004/005/006/007 are better left as awareness items for whoever next touches their respective areas, rather than a dedicated cleanup pass.
