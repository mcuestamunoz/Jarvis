# SYS-MAP-003 — Verification Matrix (Part A)

**Date:** 2026-08-10
**Scope:** Re-verify `docs/system_map/**` against current `src/jarvis/` code, tests, and re-run probes. Source-of-truth order: code → tests → runtime/CLI evidence → architecture docs → JES contracts.
**Result:** 18 CONFIRMED, 2 FIXED IN MAP (new mismatches M-003, M-004 found and corrected in docs only). Zero NEW MISMATCH left unresolved, zero NEEDS ENGINEER on the core checklist. One informational note (not a checklist item) about external design-doc activity concurrent with this audit — see bottom.

| # | Check | Result | Evidence |
|---|---|---|---|
| **A1** | Canonical registry has exactly 57 unique `C-xxx` | **CONFIRMED** | `awk '/## Canonical registry/,/## Forbidden transitions/' CONNECTIONS.md \| grep -c "^\| C-"` → `57` |
| **A2** | Status rollup 52🟢/4🔴/1🟡 | **CONFIRMED** | Same range, grep by emoji → `52 / 4 / 1` |
| **A3** | Forbidden list = 8, no `C-xxx` IDs | **CONFIRMED** | `sed -n '102,111p' CONNECTIONS.md \| grep -c "NOT IMPLEMENTED"` → `8`; none carry a `C-` prefix |
| **A4** | Derived tables labeled as derived, not counted | **CONFIRMED** | `CONNECTIONS.md` has explicit "Document structure (count correctly)" + "Derived detail (may repeat C-xxx)" section headers; derived summary tables (C-021…024, C-084/085, C-093/094) sit under that heading |
| **A5** | `DIAGRAMS.md` + `jarvis-system-map.canvas.tsx` match the 57 IDs, no drift | **CONFIRMED** | Python set-diff of IDs extracted from Canonical registry vs. `DIAGRAMS.md`'s "Registry by band" vs. canvas `CONNECTIONS` array: all three sets identical (57/57/57, zero symmetric difference). Canvas status rollup (`status: "connected"/"broken"/"partial"` counts) also 52/4/1, matching exactly. |
| **A6** | **C-042** re-traced: `"explora opciones"` → `resolve_intent`, `resolve_explore_goal`, CTA text | **CONFIRMED** | `resolve_intent("explora opciones") == "explore_design_space"`; `resolve_explore_goal("explora opciones") == None` (both re-run fresh, not assumed). `_handle_engineering_intent`'s CTA (`orchestrator.py:2283-2284`) still unconditionally reads `"... o 'explora opciones' ..."` — grep-confirmed present in current code. |
| **A7** | **C-025/C-044** re-traced: `"ayudame a mejorar la estabilidad"` → analyze; `is_engineering_intention` would detect if reached; single root, two IDs intentional | **CONFIRMED** | `resolve_intent("ayudame a mejorar la estabilidad") == "analyze"`; `is_engineering_intention("ayudame a mejorar la estabilidad") == "mejorar_estabilidad"` (re-run fresh). `CONNECTIONS.md` C-044's row text still reads `"(= C-025, cross-ref)"` — cross-ref intentional, not double-counted (confirmed against A1's 57 count). |
| **A8** | **C-043** re-traced: plan lever (`incrementa safety_factor`) → iterate, no preseed | **CONFIRMED** | Fresh orchestrator run: `"incrementa safety_factor"` → `"sí"` → `iteration_draft.variable == None`, `semantic_state.missing_slots == ["variable"]`, question `"¿Qué quieres modificar? ..."` — identical to SYS-MAP-002's documented evidence. |
| **A9** | **C-081** re-confirmed: Continuity stays generic on PASS+risky margin, correctly marked WEAK not falsely BROKEN | **CONFIRMED** | Fresh `build_project_continuity` call, `safety_margin_ratio=1.08`, architecture 4/4, no incomplete/missing components → `next_useful_step == "Diseño en PASS — puedes iterar, explorar alternativas o documentar el cierre."` (identical generic text, not wrong, just uninformative — 🟡 is the correct status, not 🔴). |
| **A10** | `ActionPolicy.ALLOWED_ACTIONS` still closed 4-verb set | **CONFIRMED** | `action_policy.py:15-20` — exactly `{CREATE_PROJECT, ITERATE, CALCULATE, SIMULATE}`, unchanged. |
| **A11** | Intent precedence in `AUTHORITY.md` matches `_resolve_strong_action_intent`'s actual order | **CONFIRMED** | Re-read `intent_resolver.py:472-516` line by line: GUIDANCE → ANALYZE → CALCULATE → SIMULATE → DEFINE_PARAMS → DISMISS_SUGGESTION → APPLY → EXPLORE → ITERATE → CREATE. Exact match to `AUTHORITY.md`'s documented 10-step order. |
| **A12** | FN-022 gate still only on `intent ∈ {iterate, unknown}` | **CONFIRMED** | `orchestrator.py:894`: `if intent in ("iterate", "unknown") and self._has_active_project():` — unchanged, this is exactly why C-025 stays broken (a phrase resolving to `"analyze"` never reaches this gate). |
| **A13** | FN-023 guidance patterns beat bare `ayúdame` for next-step phrasing, without claiming to fix C-025 | **CONFIRMED** | `intent_resolver.py` GUIDANCE_PATTERNS still contains the FN-023 block (`\bayudame\b.*\bsiguiente\s+paso\b`, etc.) — these are anchored to "siguiente paso"/"seguir" wording specifically, not to a bare named goal, so they correctly do not overlap with (and do not claim to fix) C-025's "ayúdame + goal name" shape. |
| **A14** | Checkpoint count in `RUNTIME_MAP.md` (~25) vs. `_handle_user_text_inner` | **CONFIRMED** | `_handle_user_text_inner` still spans `orchestrator.py:577-939` exactly (verified: `start_define_missing_params` begins at `:943`, immediately after) — identical range to what `RUNTIME_MAP.md` cites. No drift since SYS-MAP-002 (no `src/` edits occurred in between). |
| **A15** | Dual-dispatch C-016 still accurate | **CONFIRMED** | `orchestrator.py:257` (`handle()`'s own `self.router.resolve(...)`) and `:901-904` (`_handle_user_text_inner`'s handoff into `self.handle(...)` for the 4-action intent set) both re-read, unchanged. |
| **A16** | Continuity/Acquisition shared next-gap read (C-036) still holds | **CONFIRMED** | `grep -n "_next_pending_block(" orchestrator.py` → 7 call sites, spanning both Acquisition-side (`_try_start_acquisition_from_mention`, `_try_reprompt_active_block_declaration`, `_try_help_define_pending_idle`, `_set_pending_next_block`) and Continuity-side (`build_startup_context`) — same single function, no fork. |
| **A17** | Component write single point (C-091) still holds | **CONFIRMED** | `grep -rn "design_properties.components\[" src/jarvis` → zero matches outside `component_writers.py` (which itself has none of that exact literal pattern either — writes go through `model_copy`/dict update inside the writer functions). `apply_components_delta` (used by `design_explorer.py` and `orchestrator.py`) is defined in and only in `component_writers.py` — both callers go through it, not around it. |
| **A18** | FLOW-001…007 match code for happy paths | **CONFIRMED** | Symbol-existence + importability check for the key functions each flow cites (`_try_start_acquisition_from_mention`, `_continue_block_acquisition`, `resolve_acquisition_mention`, `build_acquisition_brief`, `CalculateAction`, `SimulateAction`, `JarvisLLMInterface.interpret/analyze`) — all present. Combined with the full **1558-test** suite passing unchanged (these flows are exercised continuously by that suite), this is sufficient evidence for the fully-🟢 flows without re-running full CLI transcripts for each. FLOW-002/003/004's broken notes were separately re-verified in A6/A8 above. |
| **A19** | `MISMATCHES.md` H1–H5 still design-only; no accidental "just implement this" recipe | **CONFIRMED, with an observation** | The H1–H5 table rows (`MISMATCHES.md` §"H1–H5 (absorbed, refined...)") are unchanged in substance and still phrased as refinements/constraints, not code. **Observation (not a FAIL):** the section's status pointer above it now reads "§5 CLOSED... First implementation cut: FN-024" — see the note at the bottom of this matrix. |
| **A20** | Open lifecycle questions still open — not closed in this cut | **CONFIRMED for this contract's own actions.** I did not close any open question, add any session field, or write any implementation code. **Externally, §5 was closed by an Engineer decision recorded in `HANDOFF_CONTEXT_DESIGN.md` concurrent with this audit** (not part of SYS-MAP-003's work) — see note below. | `git status --short -- src/` empty throughout this session; `MISMATCHES.md`'s open-questions body text (lines ~77-90) was deliberately left unedited by me, since rewriting it to reflect the closure would mean interpreting/restating an H1-adjacent decision, which is out of this contract's scope. |

## Extra checks performed beyond A1–A20 (found during verification, corrected)

| # | Check | Result | Evidence |
|---|---|---|---|
| A21 | `semantic_intent_adapter.py`'s role, as described in `10_llm/LLM_MAP.md` | **FIXED IN MAP** (new mismatch M-003) | The prior description ("used by Iteration's slot-filling... not by interpret()'s routing path") conflated it with the differently-named `core/semantic_interpreter.py`. Real callers: `llm_client.py::_build_semantic_trace` (logging only) and `orchestrator._semantic_preseed` (`:249`, wizard-start preseed, called from `handle()`'s `ITERATE` branch). Corrected in `10_llm/LLM_MAP.md` and `05_iteration/ITERATION_MAP.md`. No authority violation — the mechanism is bounded (only adjusts wizard starting step). |
| A22 | `06_calculation/CALCULATION_MAP.md` and `07_simulation/SIMULATION_MAP.md` module lists, cross-checked against actual imports | **FIXED IN MAP** (new mismatch M-004) | `tools/materials.py`, `tools/math_utils.py`, `simulation/flight_model.py`, `simulation/energy_model.py` were listed as active/consumed modules in SYS-MAP-002. All four are **0-byte empty files with zero imports anywhere** (verified via `find src/jarvis -name "*.py" -size 0` cross-referenced with per-module grep across `src/` and `tests/`). Corrected in both subsystem maps and `README.md`'s subsystem index. Five more empty, unimported modules were found during the same sweep and cataloged in Part B (never previously claimed as "used" by any map file, so no map correction needed for those five — see `sys_map_003_hygiene_inventory.md` HYG-008…016). |

## Note on `HANDOFF_CONTEXT_DESIGN.md` (informational, not a verification failure)

While this audit was in progress, `docs/system_map/HANDOFF_CONTEXT_DESIGN.md` and `MISMATCHES.md`'s pointer to it were updated externally (by the Engineer/JES process, not by this contract's work) to record: **§5 lifecycle CLOSED — "Hybrid Operation-Scoped Context"**, with **FN-024 (H1+H2, closes C-042) already issued as an Implementation Contract**. This is consistent, internally (the design doc's own Decision Log section matches its status line), but it means:

- `MISMATCHES.md`'s "Open questions" body text (the L1–L6 lifecycle candidates, "still open" framing) is now **stale relative to its own pointer note** — flagged inline in `MISMATCHES.md` for a future pass to reconcile, not fixed here (reconciling it would mean restating an H1-adjacent decision, out of SYS-MAP-003's scope).
- This is **not** something SYS-MAP-003 did or was asked to do — it happened concurrently, outside this contract's actions. `git status --short -- src/` confirms zero product code changes came from this session regardless.
- Recommend Cursor/Engineer treat `HANDOFF_CONTEXT_DESIGN.md` as the current source of truth for §5's status over `MISMATCHES.md`'s unedited open-questions prose, until that reconciliation happens.
