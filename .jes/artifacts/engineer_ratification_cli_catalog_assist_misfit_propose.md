# Engineer Ratification — CLI catalog-assist + misfit propose (investigation)

**Date:** 2026-09-01  
**Authority:** Engineer (“ratifico” after PRODUCT_SCOPE walk + three-tier notes)  
**Walk:** [engineer_cli_walk_block_closure_product_scope.md](engineer_cli_walk_block_closure_product_scope.md)  
**Notes (hypotheses):** [engineer_notes_cli_propose_on_misfit.md](engineer_notes_cli_propose_on_misfit.md)  
**Contract:** [investigation_contract_cli_catalog_assist_misfit_propose.md](investigation_contract_cli_catalog_assist_misfit_propose.md)  
**Baseline:** tag **`v0.3.5`** / **`checkpoint-phase25-hover-energy`** (`fc46938`) plus live tree (Block Closure B-PROP-ENERGY in product; Combo A typed-SKU walk **abandoned**)

---

## Ratification status

**LOCKED.** This investigation is the next queue item. Not an IC. Not Catalog Foundation. Not H5 / Option B / Structure / Conversation Engine.

Cursor writes the contract and reviews the report. Claude investigates and writes the report. **No `src/` until a later IC after Cursor review + Engineer ★ on the recommended first-IC tier.**

---

## ★ Decisions (locked for this investigation)

| ★ | Decision |
|---|---|
| **★1** | **This investigation is next.** Spine: when a bound combo no longer fits, Jarvis should propose — not only name a gap. Watts CTA, D8 vs Discrete OP, post-bind `ayúdame`, and `GAP-MOTOR-CATALOG-UNRESOLVED` vocabulary are **in** the same contract as hygiene / adjacent seams, not parallel investigations. |
| **★2** | **Three tiers are mandatory in the report.** Tier 1 = re-offer G22 list + re-bind. Tier 2 = named relax of inherited KV/prop (motor family; frankenstein risk if prop stays bound). Tier 3 = coherent motor+prop+battery combo that would sim-PASS. The report recommends **exactly one first-IC shape**: Tier 1 only, **or** Tier 1 + named G22 second pass (Tier 2) **iff** Tier 1 is empty/useless on the walk fixture. **Tier 3 is not the first IC.** |
| **★3** | Cursor notes are **hypotheses**. Verify or refute with `file:line` / live fixture. Do not copy notes as the report. |
| **★4** | **Frozen:** H5 / C-081, G24-B `_score_candidate` (unless the report proves it **blocks** the recommended first IC — then stop, do not unlock), Catalog Foundation ESC SKUs, Option B ERF, Structure / frame-mass iterate, P26 / P27-A, HD-001/002/003, Conversation Engine / Decision Engine, new domain modules, `_derive_overall` / `ASSEMBLY_READY`, catalog JSON expansion, Block Closure rollup formula. |
| **★5** | **No silent G22 fallback.** A second search pass, if recommended, must be **named** in CLI (not a KV-only dump that disagrees with the gap). G22 “empty means empty” stays unless ★ after this report says otherwise. |

---

## Next

Claude executes [investigation_contract_cli_catalog_assist_misfit_propose.md](investigation_contract_cli_catalog_assist_misfit_propose.md). Cursor reviews. Then Engineer ★ on the recommended first-IC tier → Cursor writes IC.
