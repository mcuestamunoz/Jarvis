# Next steps — See motors, then join in space

**Date:** 2026-09-09  
**Authority:** Engineer ★ “los dos, primero ver los motores y después juntar en el espacio”  
**Status:** ★2 **B1-visor-x** → [IC](implementation_contract_geometry_visor_x_stations_b1.md) APPROVED. ★1 rebind REVIEWED @ **2555**.  
**Parents:** hélices visor copies **CLOSED** + ACCEPT @ **2550** · motor visor copies **CLOSED** @ **2473** · motor envelope N2 · height cited **CLOSED** (S-row only)

---

## Sequence (locked)

```text
★1  Ver motores en el visor     ← NOW (identity / Ø, not copies)
★2  Juntar en el espacio        ← AFTER ★1 smoke (stations)
★3  Remaining pieces            ← later (battery, sensors, kit)
```

Do **not** start ★2 while motors still have no solid. Do **not** fold ★2 into ★1.

---

## ★1 — Ver motores

Copies already ship. Live SKU `emax_rs2205_2300` has **no** `diameter_mm`. Sibling `emax_rs2205s_2300` already has cited Ø **27.9** / height **31.7**. They are **different catalog rows** (envelope N2). Silent copy of 27.9 onto the no-s SKU stays **forbidden**.

| ★ | Meaning | Physics on live demos |
|---|---|---|
| **`B1-rebind`** | **Default / IC.** `cambiar motor` → sourced `emax_rs2205s_2300`. Existing `_solid_copies` draws N motor disks (5min→4, 10min→3). Disk stays disk (no cylinder). Tests glue bind→visor; `src/` only if that seam fails. | **Yes** — thrust / W / hover follow the RaceSpec row |
| **`B1-cite`** | Seed Ø on `emax_rs2205_2300` from a **page for that row**. Investigation if no URL. | No, if bind+refresh only fills dims |
| **Copy 27.9 from the S sibling** | **Forbidden** | Invents Ø on a mute SKU |

Smoke ★1 (if `B1-rebind`): one motors card; N yellow motor disks in the **row**; hélices still N disks; still not an X of 230.

---

## ★2 — Juntar en el espacio (parked)

After ★1 ACCEPT: investigation (not a surprise IC) for **B1-stations** — N distinct millimetre offsets on **one** spec, without N BOM nodes, without silent quad-X of 230, without disk-as-pose-origin unless separately ★.

Today: copies **strip** pose; writer **rejects** disk origin; frame still **no** box.

---

## Explicitly not this sequence

Cylinder · invent Rooster L×W · default-4 · `"cabe"` on disks · Conversation Engine · remaining pieces this week
